# Docker Compose file for Alfresco ACS Community Edition - PRODUCTION
# Configured for domain: dms.eris.cv
# Uses existing Nginx Proxy Manager (no Traefik)
#
# Prerequisites:
# 1. Domain dms.eris.cv pointing to your VPS IP
# 2. Nginx Proxy Manager running on ports 80/443
# 3. At least 6GB RAM available
#
# Usage:
#   docker compose -f production-compose-nginxpm.yaml up -d
#
# Then configure in Nginx Proxy Manager:
# - Domain: dms.eris.cv
# - Forward to: localhost:8080
# - SSL: Enable Let's Encrypt

services:
  alfresco:
    image: docker.io/alfresco/alfresco-content-repository-community:25.3.0
    mem_limit: 1900m
    environment:
      JAVA_TOOL_OPTIONS: >-
        -Dencryption.keystore.type=JCEKS
        -Dencryption.cipherAlgorithm=DESede/CBC/PKCS5Padding
        -Dencryption.keyAlgorithm=DESede
        -Dencryption.keystore.location=/usr/local/tomcat/shared/classes/alfresco/extension/keystore/keystore
        -Dmetadata-keystore.password=mp6yc0UD9e
        -Dmetadata-keystore.aliases=metadata
        -Dmetadata-keystore.metadata.password=oKIWzVdEdA
        -Dmetadata-keystore.metadata.algorithm=DESede
      JAVA_OPTS: >-
        -Ddb.driver=org.postgresql.Driver
        -Ddb.username=alfresco
        -Ddb.password=alfresco
        -Ddb.url=jdbc:postgresql://postgres:5432/alfresco
        -Dsolr.host=solr6
        -Dsolr.port=8983
        -Dsolr.http.connection.timeout=1000
        -Dsolr.secureComms=secret
        -Dsolr.sharedSecret=secret
        -Dsolr.base.url=/solr
        -Dindex.subsystem.name=solr6
        -Dshare.host=dms.eris.cv
        -Dshare.port=443
        -Dalfresco.host=dms.eris.cv
        -Dalfresco.port=443
        -Dcsrf.filter.enabled=false
        -Daos.baseUrlOverwrite=https://dms.eris.cv/alfresco/aos
        -Dmessaging.broker.url="failover:(nio://activemq:61616)?timeout=3000&jms.useCompression=true"
        -Ddeployment.method=DOCKER_COMPOSE
        -DlocalTransform.core-aio.url=http://transform-core-aio:8090/
        -XX:MinRAMPercentage=50
        -XX:MaxRAMPercentage=80
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/alfresco/api/-default-/public/alfresco/versions/1/probes/-ready-"]
      interval: 30s
      timeout: 3s
      retries: 5
      start_period: 1m
    volumes:
      - alfresco_data:/usr/local/tomcat/alf_data
    ports:
      - "8080:8080"  # Expor para Nginx Proxy Manager
    networks:
      - alfresco-network

  transform-core-aio:
    image: alfresco/alfresco-transform-core-aio:5.2.4
    mem_limit: 1536m
    environment:
      JAVA_OPTS: >-
        -XX:MinRAMPercentage=50
        -XX:MaxRAMPercentage=80
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/ready"]
      interval: 20s
      timeout: 2s
      retries: 3
      start_period: 10s
    depends_on:
      activemq:
        condition: service_healthy
    networks:
      - alfresco-network

  share:
    image: docker.io/alfresco/alfresco-share:25.3.0
    mem_limit: 1g
    environment:
      CSRF_FILTER_ORIGIN: https://dms.eris.cv
      CSRF_FILTER_REFERER: https://dms.eris.cv/share/.*
      REPO_HOST: "alfresco"
      REPO_PORT: "8080"
      JAVA_OPTS: >-
        -XX:MinRAMPercentage=50
        -XX:MaxRAMPercentage=80
        -Dalfresco.host=dms.eris.cv
        -Dalfresco.port=443
        -Dalfresco.context=alfresco
        -Dalfresco.protocol=https
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/share"]
      interval: 20s
      timeout: 2s
      retries: 3
      start_period: 15s
    depends_on:
      alfresco:
        condition: service_healthy
    ports:
      - "8081:8080"  # Expor para Nginx Proxy Manager (porta diferente)
    volumes:
      # Montar personalizações do logo
      # Criar pasta /opt/alfresco/share-logo no servidor e colocar app-logo.png lá
      - /opt/alfresco/share-logo:/usr/local/tomcat/webapps/share/res/themes/default/images:ro
      # Montar extensões customizadas (opcional)
      - /opt/alfresco/share-custom:/usr/local/tomcat/shared/classes/alfresco/web-extension:ro
    networks:
      - alfresco-network

  postgres:
    image: postgres:16.5
    mem_limit: 512m
    environment:
      - POSTGRES_PASSWORD=alfresco
      - POSTGRES_USER=alfresco
      - POSTGRES_DB=alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $$POSTGRES_DB -U $$POSTGRES_USER"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - alfresco-network

  solr6:
    image: docker.io/alfresco/alfresco-search-services:2.0.17
    mem_limit: 2g
    environment:
      SOLR_ALFRESCO_HOST: "alfresco"
      SOLR_ALFRESCO_PORT: "8080"
      SOLR_SOLR_HOST: "solr6"
      SOLR_SOLR_PORT: "8983"
      SOLR_CREATE_ALFRESCO_DEFAULTS: "alfresco,archive"
      ALFRESCO_SECURE_COMMS: "secret"
      JAVA_TOOL_OPTIONS: >-
        -Dalfresco.secureComms.secret=secret
    networks:
      - alfresco-network

  activemq:
    image: alfresco/alfresco-activemq:5.18-jre17-rockylinux8
    mem_limit: 1g
    healthcheck:
      test: ["CMD", "/opt/activemq/bin/activemq", "query", "--objname", "type=Broker,brokerName=*,service=Health", "|", "grep", "Good"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - alfresco-network

volumes:
  alfresco_data:
    driver: local
  postgres_data:
    driver: local

networks:
  alfresco-network:
    driver: bridge

